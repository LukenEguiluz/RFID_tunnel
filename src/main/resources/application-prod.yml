spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:postgres}:${DB_PORT:5432}/${DB_NAME:rfidgateway}
    username: ${DB_USER:rfiduser}
    password: ${DB_PASSWORD}

logging:
  level:
    root: WARN
    com.rfidgateway: INFO





